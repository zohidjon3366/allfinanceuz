const express=require('express');const path=require('path');const app=express();const PORT=process.env.PORT||3000;app.use(express.json({limit:'1mb'}));app.use(express.urlencoded({extended:true}));app.use(express.static(__dirname,{extensions:['html']}));app.post('/api/lead',async(req,res)=>{const token=process.env.TELEGRAM_BOT_TOKEN||'';const chat=process.env.TELEGRAM_CHAT_ID||'';if(!token||!chat)return res.status(200).json({ok:false,message:'Telegram sozlanmagan'});const d=req.body||{};const text=['🔔 Yangi murojaat — ALL FINANCE','',`Ism: ${d.name||'-'}`,`Telefon: ${d.phone||'-'}`,`Korxona: ${d.company||'-'}`,`Xizmat: ${d.service||'-'}`,`Izoh: ${d.comment||'-'}`,`Sana: ${new Date().toLocaleString('ru-RU')}`].join('\n');try{const r=await fetch(`https://api.telegram.org/bot${token}/sendMessage`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({chat_id:chat,text})});const j=await r.json().catch(()=>({}));return res.status(200).json({ok:!!(r.ok&&j.ok)});}catch(e){return res.status(200).json({ok:false});}});app.use((req,res)=>res.status(404).sendFile(path.join(__dirname,'404.html')));app.listen(PORT,()=>console.log(`ALL FINANCE started on ${PORT}`));